// this is a line added to see if git creates a different hash for the same file if the file contents changes.
function main() {
	console.log("This is  a sample file that we will be adding inside the git for checking how git stores this file in the .git/objects directory");
}

main();
